import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';  // Converted require to import for consistency
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
